language: go

go:
 - "1.10.2"

before_install:
  - go get -t -v ./...

script:
  - go test -v ./core -coverprofile=coverage.txt -covermode=atomic

after_success:
  - bash <(curl -s https://codecov.io/bash)
